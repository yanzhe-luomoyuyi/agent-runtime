# Requirement

`getUserName(session)` must return `"anonymous"` when `session` is `null` or `undefined`, instead of throwing.

After fixing, run tests and write `ANALYSIS.md` describing the root cause and the change.
