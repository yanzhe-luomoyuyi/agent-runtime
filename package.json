{
  "name": "agent-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo: @agent/contracts (seam), @agent/harness (agent loop), durable-agent-runtime (durable host).",
  "workspaces": [
    "agent-contracts",
    "agent-harness",
    "durable-agent-runtime"
  ],
  "scripts": {
    "build": "npm run build -w @agent/contracts && npm run build -w @agent/harness && npm run build -w durable-agent-runtime",
    "build:contracts": "npm run build -w @agent/contracts",
    "typecheck": "npm run build -w @agent/contracts && npm run typecheck -w @agent/harness && npm run typecheck -w durable-agent-runtime",
    "test": "npm run build -w @agent/contracts && npm run test -w @agent/harness && npm run test -w durable-agent-runtime"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
