/**
 * Demo model responses for the fixed workflow — re-exports the shared fixtures.
 * Kept as a thin module so existing imports (`./responses.js`) stay stable.
 */

export { cannedResponses, DEMO_PROPOSALS, proposeForGoal } from './demo-fixtures.js';
